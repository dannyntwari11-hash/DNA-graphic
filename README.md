# DNA Graphics
Fixed, clean static website for GitHub Pages.

## GitHub Pages
Settings → Pages → Deploy from a branch → `main` → `/ (root)` → Save.

The site uses only HTML, CSS and JavaScript, so no build command is required.
